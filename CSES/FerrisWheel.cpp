#include <bits/stdc++.h>
using namespace std;
typedef size_t ll;

int main() {
    ll N, X; cin >> N >> X;

    vector<ll> children (N);
    for (ll& c : children) cin >> c;

    ll out = 0;
    for (ll i = 0; i <= ceil(N / 2.0); i++) {
        if (i == N / 2.0) break;
        ll j = N - i - 1;

        
    }
}